{% extends 'base.html.twig' %}

{% block title %}Lugar{% endblock %}

{% block body %}
    <h1>Lugar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lugar.id }}</td>
            </tr>
            <tr>
                <th>Monumento</th>
                <td>{{ lugar.monumento }}</td>
            </tr>
            <tr>
                <th>Latitud</th>
                <td>{{ lugar.latitud }}</td>
            </tr>
            <tr>
                <th>Longitud</th>
                <td>{{ lugar.longitud }}</td>
            </tr>
            <tr>
                <th>Imagen</th>
                <td>{{ lugar.imagen }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lugar_index') }}">back to list</a>

    <a href="{{ path('lugar_edit', {'id': lugar.id}) }}">edit</a>

    {{ include('lugar/_delete_form.html.twig') }}
{% endblock %}
